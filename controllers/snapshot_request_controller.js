const Client = require('../models/clients.js');

//  saves snapshot to DB and sends success message to user
const saveSnapshotRequests = (req, res, next) => {
};

module.exports = {
  saveSnapshotRequests,
};
